# tools

Language tooling stubs and helper scripts for `t81-lang`.

Planned tools:

- CLI binary (`t81-lang parse <file.t81>`, `t81-lang check <file.t81>`) built via `scripts/build-t81-lang-cli.sh`
- REPL (`t81-lang repl`)
- Formatter (`t81fmt`)
- Linter (`t81lint`)
- Playground helpers
