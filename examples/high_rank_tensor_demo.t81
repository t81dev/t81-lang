fn high_rank_tensor_demo() -> i32 {
    let block: Tensor[i32, 2, 2, 2] = [
        [[1, 2], [3, 4]],
        [[5, 6], [7, 8]]
    ];

    let corner = block[0][0][0];
    let opposite = block[1][1][1];
    let total = corner + opposite;

    return total;
}

fn main() -> i32 {
    return high_rank_tensor_demo();
}
